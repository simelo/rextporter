documment over the [examples](https://github.com/denisacostaq/rextporter/blob/master/examples) about the meaning configs.

usar un archivo de configuracion en hom por defecto si no existe

usar varios archivos de ejemplo de configuracion no comentar, descometar seguidamente.


configurar un node de skycoin


replace strings.Compare(varName, "") == 0 by len(varName) == 0